# ---------- Shared (rename to .env locally; never commit .env) ----------
# BACKEND
API_PORT=4000
JWT_SECRET=change_me
# FRONTEND
VITE_API_BASE_URL=http://localhost:4000

# AGORA (use this in /agora and frontend)
AGORA_APP_ID=your_agora_app_id
AGORA_TEMP_TOKEN=your_temp_token   # use only in dev if needed

# AI
OPENAI_API_KEY=sk-xxxx
